on: workflow_call
jobs:
  myjoba:
    strategy:
      matrix:
        os:
#         - ubuntu-latest
#         - ubuntu-latest
#         - ubuntu-latest
        - selfhoster
    runs-on: ${{matrix.os}}
    steps:
    - run: echo Hello World
      shell: cmd
      if: env.ACT
    - run: echo '${{toJSON(github)}}'
      shell: powershell
      if: env.ACT
    - run: echo Hello World
      if: "!env.ACT"
